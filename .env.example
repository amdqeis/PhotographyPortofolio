# Server Configuration
PORT=3001

# PostgreSQL Database Configuration
# Option 1: Full Connection String (takes priority if provided)
# DATABASE_URL=postgresql://postgres:postgres@localhost:5432/webportofolio

# Option 2: Individual Connection Parameters
PGHOST=localhost
PGPORT=5432
PGUSER=postgres
PGPASSWORD=postgres
PGDATABASE=webportofolio

# Authentication & Security
CMS_ADMIN_PASSWORD=your_secure_admin_password
JWT_SECRET=your_jwt_secret_key_change_in_production
